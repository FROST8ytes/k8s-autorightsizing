---
apiVersion: v1
kind: Service
metadata:
  name: idle-app
  namespace: default
  labels:
    app: idle-app
    component: sample-app
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8080
    targetPort: http
    protocol: TCP
  selector:
    app: idle-app
